debug: true  # 打开调试模式
server:
  port: 8080
spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/test?useSSL=false&serverTimezone=GMT%2B8
  devtools:
    restart:
      enabled: true
      #排除那个目录的文件不需要restart
  #            exclude: static/**,public/**
  jpa:
    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true

